import { FishingEventOverview } from './fishing-event-overview';

describe('FishingEventOverview', () => {
  it('should create an instance', () => {
    expect(new FishingEventOverview()).toBeTruthy();
  });
});
